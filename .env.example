# Database URL (SQLite for local development)
# On Vercel, the app uses fallback static data when the database is unavailable.
# Set this to any valid SQLite path so Prisma Client can be generated.
DATABASE_URL=file:./dev.db
