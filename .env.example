# ─── Database ────────────────────────────────────────────────────────────────
# Local development (SQLite):
DATABASE_URL=file:./db/custom.db
# Docker/Production (PostgreSQL):
# DATABASE_URL=postgresql://accessmap:accessmap_secret@db:5432/accessmap

# ─── Authentication (Auth.js v5) ─────────────────────────────────────────────
# Required: Generate with `openssl rand -base64 32`
AUTH_SECRET=change-me-to-a-secure-random-string

# ─── Admin (Legacy, no longer used with NextAuth) ────────────────────────────
# ADMIN_PASSWORD=accessmap2024

# ─── Auth URL (production only) ──────────────────────────────────────────────
# AUTH_URL=https://your-domain.com
